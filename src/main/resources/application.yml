server:
  port: 1112
  servlet :
    context-path: /
    encoding:
      charset: utf-8
      enabled: true
spring:
  mvc:
    view:
      suffix: .html

  servlet:
    multipart:
      enabled: true
      max-file-size: 2MB
